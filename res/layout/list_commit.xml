<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

     <TextView
         android:id="@+id/CommitNumber"
         android:layout_width="wrap_content"
         android:layout_height="fill_parent"
         android:layout_alignParentBottom="true"
         android:layout_alignParentTop="true"
         android:layout_marginRight="6dip"
         android:gravity="center_vertical|right"
         android:text="132"
         android:textAlignment="center"
         android:textColor="#000066"
         android:textSize="16sp"
         android:textStyle="bold" />

    <TextView
        android:id="@+id/CommitDate"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/CommitNumber"
        android:ellipsize="marquee"
        android:padding="0dp"
        android:singleLine="true"
        android:text="May 13, 2014 4:40:02 PM"
        android:textColor="#707070"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/CommitTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/CommitDate"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/CommitNumber"
        android:gravity="center_vertical"
        android:maxLines="1"
        android:padding="0dp"
        android:text="Example application très très long tout ca"
        android:textSize="14sp" />

</RelativeLayout> 